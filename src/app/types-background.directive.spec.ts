import { TypesBackgroundDirective } from './types-background.directive';

describe('TypesBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new TypesBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
